local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Vlad = require(ReplicatedStorage.Common.Vlad)

local Components = {
    Name = "Components",
    Utility = "Extension"
}

function Components:Initialize()
    --Something here
end

function Components:FrameworkStarted()
    
end

return Components